# Runtime-only dependencies for the production Docker image.
# Kept in sync manually with the versions pinned in requirements.txt --
# excludes the "Dev / test tooling" section (pytest, ruff, factory_boy, ...)
# so the prod image doesn't ship test/lint tooling.

# Core framework
Django==6.0.7
djangorestframework==3.17.1
asgiref==3.11.1
sqlparse==0.5.5

# Auth
djangorestframework-simplejwt==5.5.1
PyJWT==2.13.0
PyOTP==2.10.0

# Config / infra
django-environ==0.14.0
django-cors-headers==4.9.0
django-simple-history==3.12.0
drf-spectacular==0.30.0
psycopg2-binary==2.9.12

# Production server
gunicorn==23.0.0
